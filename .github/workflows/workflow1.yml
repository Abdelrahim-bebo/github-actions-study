name: Workflow 1

on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a script
        run: |
          echo "Hello from Workflow 1!"
          ls -ltra
          cat README.md

      - name: Generate Ascii Artwork
        run: cowsay -f dragon "Run for cover, I am a DRAGON....RAWR" >> dragon.txt
